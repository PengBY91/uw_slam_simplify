# HoloOcean 仿真工作交办说明

> 2026-09-07 注记：仓库主线二（ROV 实时闭环）已整体剥离，快照在
> `archive/rov-realtime-line2` 分支。本文交办的实时闭环仿真工作随之中停；
> HoloOcean 仿真**录制**路径（`adapters/holoocean` 录制子集）仍在 main 上维护，
> 相关任务可继续按本文执行。

> 交办日期：2026-09-02 · 交办人：技术负责人 · 接手人：仿真工程师（下称"你"）
> 范围：`docs/ROV平台到货前准备工作规格-2026-09-02.md`（下称"规格"）里的工作包 A（PREP-A-01 ～ A-13），共 13 项
> 本文只讲"做什么、按什么顺序、怎么算完成、别踩什么坑"；每项的技术细节以规格对应条目为准，不在这里重复。

---

## 1. 一句话任务

把现有 HoloOcean 仿真环境改成**我们即将到货那台 ROV 的数字孪生**，并让仿真器**按真机设备的线上格式输出数据**，使真机到货前，所有 adapter、SLAM 算法、飞控接口和飞手训练都能在仿真上先跑起来。

你的产出决定后面所有算法和 adapter 工作有没有测试台。你不是在做"仿真 demo"，是在做这两个月全团队的验证基础设施。

## 2. 五分钟背景

**硬件**（合同已签，细节在 `docs/ROV平台参数.md`）：BlueROV2 Heavy，ArduSub 飞控，供应商不给 SDK 也不做算法；**单目** IMX462 云台相机（后续会另装双目，型号未定）；外挂维特 HWT9053-485 IMU 经树莓派 USB；**无 DVL，后期也不装**；前视声呐 768 波束、140°×20°、40 Hz，只出灰度图；tether 实测 10–40 Mbps。

**这意味着**：仓库现有的双目 VO 主线在到货形态下用不了，第一阶段定位靠 IMU 预积分 + 声呐帧间配准 + 深度 + 磁航向（规格 D-1）。这些算法全部要在你搭的仿真上开发和验证。

**仿真现状**（你接手时的起点）：
- HoloOcean 2.3.0 装在 Windows 那台机器上（WSL2 里跑不起来，别再试，已经证明是死路）。
- Windows 侧 `holoocean_bridge_sensor_host.py` 持有 HoloOcean，经自定义 TCP 协议把原始帧送到 WSL2 侧 `bridged_realtime_ros_session.py`，再转成 ROS2 话题，C++ 网关 `holoocean_realtime_node` 接进管线。这条链 2026-08-27 实跑过 14 分钟，0 丢帧。
- 场景基线 `adapters/holoocean/scenarios/blue_rov_aid_sv1213_base.json`：机体已是 `BlueROV2`，世界 `PierHarbor`，但传感器是双目 20 Hz + IMU 50 Hz + 声呐 10 Hz，跟合同机器差得远。
- 实测 tick 率只有每秒 12–13 个，配置写的是 100。这是你第一周就要量清楚的事。
- 这条桥接链的运行方法**只写在三个模块的 docstring 里**（`raw_frame_wire.py`、`holoocean_bridge_sensor_host.py`、`bridged_realtime_ros_session.py`），`adapters/holoocean/README.md` 第 9 行起那段"本机没有 HoloOcean"已经过时。你上手后第一件文档工作就是把运行步骤补进 README。

## 3. 交付物与顺序

按里程碑交付，每个里程碑结束时按第 7 节格式汇报一次。周数是规格第 9 节的排期，供参考；先后顺序比周数重要。

### M1（第 1 周）：摸清边界

| 任务 | 交付物 | 完成标准 |
|---|---|---|
| A-01 GPU/tick 预算 | 5 种传感器配置的 tick 率/每 tick 墙钟/声呐渲染耗时/显存表，写进 `docs/specifications/holoocean-realtime-closed-loop-simulation-spec.md` | 表填满；写明"实时闭环仿真慢于实时多少" |
| A-02 UE5 工程可打包验证 | `adapters/holoocean/docs/ue5-world-packaging.md` + 一个能加载的空关卡 | HoloOcean Python 能加载新 world 并拿到声呐帧；文档第二个人能复现 |
| A-04 单目录制回归测试 | `adapters/holoocean/tests/test_record_session_monocular.py` | pytest 通过 |
| README 补运行步骤 | `adapters/holoocean/README.md` 更新 | 按 README 能从零把 Windows↔WSL2 桥接跑起来 |

**A-01 和 A-02 是两个决策点**，结果直接决定后面怎么做（见第 6 节）。

### M2（第 2–3 周）：数字孪生基线

| 任务 | 交付物 | 完成标准 |
|---|---|---|
| A-03 合同机器基线 | `blue_rov_contract_base.json` + `_mono.json` + `_stereo.json`；`configs/rig/bluerov2_contract.yaml`；网关/录制器改为按配置启用相机 | mono 和 stereo 各跑 5 分钟不报错；`bag_audit` 通过；现有双目回归在 stereo 配置下不退化 |
| A-10 声呐统计工具 | `adapters/holoocean/tools/sonar_stats.py` | 对现有录制可运行，两段 bag 能对比出图 |

A-03 的要点：**不删双目通路**，做成可配；航向不再用真值，加噪声和推力相关偏置；声呐两种频率各一份配置，都保持 768 bin。

### M3（第 3–4 周）：飞控闭环可行性 + 第一个关卡

| 任务 | 交付物 | 完成标准 |
|---|---|---|
| A-05 SITL–HoloOcean 桥接可行性 | `ardusub_sitl_bridge.py` + `adapters/holoocean/docs/ardusub-sitl-bridge-feasibility.md` + 录屏 | **3 个工作日时间盒**，到点无论结果都停下写结论 |
| A-06 养殖区关卡 | UE5 关卡 + `aquaculture_search.yaml` 改为引用它 | 声呐图上网箱边缘、浮球、缆绳可辨；`task_scorer.py` 能跑通一次 scripted pilot |
| A-09 相机退化 | `sensor_perturbation.py` 加 `ir_night`/`h264_recompress` + 对比表 | 有"退化类型 × 强度 → 指标"表 |

A-05 依赖 SITL 环境（规格 PREP-C-01，由 SDK/平台集成 在第 1 周搭好），你不用自己搭。

### M4（第 4–6 周）：设备伪装层 + 训练台

| 任务 | 交付物 | 完成标准 |
|---|---|---|
| A-13 伪装流（MAVLink + IMU 两路） | sensor host 加 `--backend device_emulation`；`device_emulation/mavlink_emitter.py`、`imu_udp_emitter.py` | 对应的真机 adapter 在伪装流上跑 30 分钟无错 |
| A-08 水池关卡 | UE5 关卡 + `configs/scenario/pool_<name>.yaml` | 关卡加载；稀疏控制点评测能出数 |
| A-12 飞手训练台 | 手柄→命令小节点 + 自动录制评分 | 飞手能完成一次养殖区搜索并有评分 |
| A-11 合成数据 | 标注数据集生成脚本 | 一次产出 ≥2000 张，抽检 50 张框正确 |

A-13 的 wire 格式不由你定：MAVLink 消息集和 IMU UDP 格式分别由做 PREP-C-03 和 PREP-D-02 的人定，你负责按定好的格式发。**外部时钟源模式要动核心层 `LiveEventSource`，那部分由技术负责人做**，你只保证伪装流的时间戳是仿真时间并且 MAVLink 流里有 `SYSTEM_TIME`。

### M5（第 7–8 周）：收尾

| 任务 | 交付物 | 完成标准 |
|---|---|---|
| A-07 结构物关卡 | UE5 关卡 | 路径评分可跑 |
| A-13 伪装流（声呐 + 相机两路） | `sonar_emitter.py`、`camera_gst_emitter.py` | 同上 30 分钟无错；带宽整形到 10 Mbps 时降级顺序符合规格 E-01 |

声呐伪装的内部格式要等厂家回复图像格式（规格 PREP-F-02 第一条），回复之前不要动手定格式。

## 4. 每项任务先读什么

| 任务 | 先读 |
|---|---|
| 全部 | `CLAUDE.md`（硬性规则）、规格第 0–2 节、`adapters/holoocean/README.md`、`docs/specifications/holoocean-realtime-closed-loop-simulation-spec.md` |
| A-01/A-03 | `adapters/holoocean/scenarios/blue_rov_aid_sv1213_base.json`、`scenario_manifest.py`（含真值隔离校验）、`realtime_ros_session.py`、`record_session.py` |
| A-02/A-06/A-07/A-08 | HoloOcean 2.3.0 官方文档的 world/prop 部分；`adapters/holoocean/scenarios/*.yaml` 的 `props`/`acoustic_reflectivity_class` 字段 |
| A-04 | `record_session.py` 顶部 docstring、`tests/test_record_session.py` |
| A-05 | ArduPilot 文档 SITL JSON 接口一节；HoloOcean `agents.py` 里 `AUV_THRUSTERS`/`AUV_FORCES` 说明 |
| A-09 | `sensor_perturbation.py`、`tests/test_sensor_perturbation.py` |
| A-13 | 三个桥接模块的 docstring；规格 PREP-A-13、C-03、D-02、E-04 |

## 5. 硬性规则（违反任何一条都要返工）

1. **不改 `external_repos/` 下任何东西**，包括 `holoocean-ros`。
2. **依赖只能单向**：你的代码全部在 `adapters/holoocean/` 和 `adapters/ros2/`，`include/`、`src/` 下的核心层不许 include HoloOcean、ROS、OpenCV。改完跑 `tools/lint/check_no_ros_in_core.sh`。
3. **真值隔离**：`/uw/sim/ground_truth` 只能进评分器，任何算法输入 topic 和伪装流都不许带真值。`scenario_manifest.py` 有校验，新加配置必须过它。
4. **不用欧拉角**，位姿一律 `Pose3`（平移 + 四元数 xyzw）。
5. **随机数用显式 seed 的 RNG 传到调用点**，不许全局 seed，不许中途重 seed。
6. **不 `git commit`**，除非技术负责人明确说提交。工作树里别人的改动不许动。
7. **消息模型改 `.proto`**，不在 Python 侧另建平行 struct。
8. **不删双目通路**，一切单目改造做成可配。
9. 改完代码的验证顺序：编译 → C++ 测试 → `(cd adapters/holoocean && .venv/bin/pytest -q)` → lint → **实跑一次端到端**。仓库里有两个 bug 是单测全绿、实跑才发现的，别只信单测。

## 6. 三个决策点，结果出来立刻找我

| 决策点 | 触发 | 影响 |
|---|---|---|
| A-01 结果 | 第 1 周末 | 定合同机器基线用 1080p 还是 960×540、声呐 40 Hz 还是 20 Hz；定实时闭环仿真是否慢于实时 |
| A-02 结果 | 第 1 周末 | UE5 工程拿不到或打不了包 → 三个关卡降级为 `spawn_prop` 几何体，提前采购真实网箱样品 |
| A-05 结果 | 第 3 周末（3 天时间盒） | 可行 → SITL 桥接成为主仿真环境；不可行 → 控制层用 HoloOcean 推力后端 + 简易控制器 |

不要为了让结果好看延长时间盒。不可行也是有价值的结论。

## 7. 汇报格式（每个里程碑 + 每周五）

三段，不超过半页：
- **跑通了什么**：配置名、时长、数字（tick 率、ATE、丢帧数），附 bag 或截图路径。
- **卡在哪**：具体报错或现象，已经试过什么。
- **需要什么**：第 8 节里还没拿到的输入，或需要我拍板的事。

数字必须来自实跑，不写"应该可以"。

## 8. 你需要向我要的输入

| 输入 | 用在 | 状态 |
|---|---|---|
| 相机/声呐/IMU 在机体上的安装位置（CAD 或实测） | A-03 rig 外参初值 | 待给 |
| 云台锁死角度 | A-03 | 待拍板 |
| 水池尺寸与池底标记位置 | A-08 | 待要 |
| 双目型号、基线、接口 | A-03 stereo 占位值 | 待定 |
| 声呐厂家对图像格式/时间同步的答复 | A-13 声呐路、A-10 | 已发问，等回复 |
| MAVLink 消息集与 IMU UDP wire 格式 | A-13 | 由 C-03/D-02 负责人给 |
| ArduSub SITL 环境 | A-05 | C-01 第 1 周交付 |

## 9. 环境与已知的坑

- **Windows 那台机器**：HoloOcean 安装、验证脚本在 `C:\Users\pengb\uw_slam_holoocean_check\`。从 WSL2 调 Windows 侧要走 `powershell.exe`，必须用 `.py` 文件不能用 `-c` 内联，且 `-WorkingDirectory` 要给真实本地路径，否则报 WinError 4551。
- **WSL2 侧**：本机有三个 Python（conda base、`~/.local/bin` 下 uv 装的、系统 `/usr/bin/python3`）。`colcon build` 要系统 Python 在前（`PATH="/usr/bin:/opt/ros/jazzy/bin:/bin"`），`cmake -DUW_BUILD_ROS2=ON` 要 conda 的 cmake 4.x 在前。两步 PATH 顺序相反，不能一套环境变量走到底。
- **apt/rosdep 卡住**先查代理：`/etc/apt/apt.conf.d/95proxy` 和 `sudo -E`。
- **WSL2↔Windows 的 TCP 长连接会掉**，桥接里已有重连循环，别当 bug 修。
- **HoloOcean 慢于实时**是常态，所有时间戳用仿真时间，延迟指标按仿真时间算。
- **HoloOcean 声呐没有波束宽度参数**，两种频率模式的角向分辨率差异仿不出来，只能调噪声近似，记进规格条目就行。
- `adapters/holoocean/README.md` 第 9 行起"本机没有 HoloOcean"那段已过时，别被误导。

## 10. 第一天清单

- [ ] 读 `CLAUDE.md`、规格第 0–2 节、本文。
- [ ] 按三个桥接模块的 docstring 把 Windows↔WSL2 桥接链跑起来一次，看到 `holoocean_realtime_node` 有输出。
- [ ] `(cd adapters/holoocean && .venv/bin/pytest -q)` 全绿。
- [ ] 把上一步的运行步骤写进 README（这就是你第一个 diff）。
- [ ] 开始 A-01：先跑当前配置 60 秒，记下 tick 率。
